// const { Sequelize } = require("sequelize");
// const { HOST, USER, PASSWORD, DB, dialect } = require("../config/config");
// const { createUsersModel } = require("./users.model");
// const sequelize = new Sequelize(DB, USER, PASSWORD, {
//   host: HOST,
//   dialect: dialect,
// });

// const Users = createUsersModel(sequelize);

// module.exports = {
//   sequelize,
//   Users
// };
